if assignment.assignmentStatus == 'Requested for Resubmission'
    p You have recieved a request for resubmission of assignment.

else
    p Your Assignment is marked as completed.


h3 Assignment Detail
p #{assignment.assignmentTitle}
p Unit-#{assignment.assignmentUnit}
p Assignment-#{assignment.assignmentNo}

p Status-#{assignment.assignmentStatus}

h3 Remarks
p #{assignment.assignmentRemark}

h3 Course Detail

p title:#{course.courseTitle}
p duration:#{course.courseDuration} 

h3 Client Detail

p title:#{client.clientName}
p location:#{client.location}

